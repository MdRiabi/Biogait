# PostgreSQL (Docker) — démarrer : cd docker && docker compose up -d
# Identifiants alignés sur docker/docker-compose.yml
DATABASE_URL=postgresql+asyncpg://biogait:biogaitsecret@127.0.0.1:5432/biogait
REDIS_URL=redis://127.0.0.1:6379/0
SECRET_KEY=supersecretkeyforjwtsigningchangeme
DEBUG=True

# Secours dev sans Docker (décommenter et commenter DATABASE_URL ci-dessus) :
# DATABASE_URL=sqlite+aiosqlite:///./biogait.db
